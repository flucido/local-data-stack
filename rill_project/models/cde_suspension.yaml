# Model YAML
# Reference documentation: https://docs.rilldata.com/build/models

type: model
materialize: true

sql: |
  -- CDE Suspension Rate Indicator (Dashboard)
  -- Pre-computed Status / Change / Performance Color from CDE downloadable file
  -- Grain: cds_code × academic_year × student_group × aggregate_level
  SELECT * FROM read_parquet('data/cde_suspension.parquet')
