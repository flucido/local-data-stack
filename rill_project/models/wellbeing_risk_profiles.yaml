# Model YAML
# Reference documentation: https://docs.rilldata.com/build/models

type: model
materialize: true

sql: |
  -- Student Wellbeing Risk Profiles Model
  SELECT * FROM read_parquet('data/wellbeing_risk_profiles.parquet')
