# Model YAML
# Reference documentation: https://docs.rilldata.com/build/models

type: model
materialize: true

sql: |
  -- CDE English Learner Progress Indicator (ELPI)
  -- Pre-computed ELPI Status Rate / Change / Performance Color
  -- Grain: cds_code × academic_year × student_group × aggregate_level
  SELECT * FROM read_parquet('data/cde_elpac.parquet')
