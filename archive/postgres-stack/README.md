## Historical Snapshot Note

`oss_framework-docker-compose.yml` was preserved here as a historical snapshot of the old OSS Framework PostgreSQL-based Docker Compose stack.

Relative paths inside that file still reflect its original location under `oss_framework/`, so they were not rewritten when the file was archived.

This material is reference-only and unsupported.
